## Running the Project

```bash
dotnet restore
dotnet run
```

The connection string configured in `appsettings.json` is sufficient to start the project.

## Main Technologies

- .NET 9.0
- Entity Framework Core 9.0.19
- SQL Server (via EF Core provider 9.0.19)
